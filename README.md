# Simple Calculator Using HDL and a simple C++ parser

Goal: A CLI calculator that should be able to parse:

```bash
calc "1 + 2"
calc "-10 + (7 - -3)"
```

Pretty much the idea we have a quick C++ parser that parses expressions to be put into an ALU made throguh SystemVerilog to do arithmetic.


